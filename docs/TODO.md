# TimberVox TODO

This is the canonical active work list. Completed work belongs in [`../CHANGELOG.md`](../CHANGELOG.md); [`REBUILD.md`](REBUILD.md) holds the durable product and architecture roadmap.

## Now: rebuild the iPhone app deliberately

[`../apps/ios/ARCHITECTURE.md`](../apps/ios/ARCHITECTURE.md) is the source of truth for the mobile page map, Expo/native ownership, component system, shared Shortcut contract, cleanup boundary, and per-slice iPhone acceptance loop.

**Organization rule:** each numbered section below is a vertical interaction slice, not a technical subsystem bucket. A slice is complete only when its new behavior can be opened and exercised on an iPhone. Database, files, API, and native work land inside the first interaction that needs them. Each slice ends with a runnable acceptance path before the next slice starts.

### 1. Foundation slice — navigate the real app

- [x] Remove starter/demo assets and the generic reset script while preserving the keyboard, App Group bridge, realtime client, history database code, and signing evidence.
- [x] Set `supportsTablet: false`; name the iPhone simulator destination explicitly in every native build or UI-verification command.
- [x] Configure React Native Reusables in the existing Expo app with NativeWind, semantic tokens, `PortalHost`, `components.json`, generated `Text`/`Button`/`Icon` source, and a passing RNR doctor check. References: [RNR manual installation](https://reactnativereusables.com/docs/installation/manual), [NativeWind with Expo](https://www.nativewind.dev/docs/getting-started/installation).
- [x] Verify the new `NativeTabs` shell with Record, Modes, History, and Settings plus one nested `Stack` per tab. Onboarding remains outside the tab shell. Acceptance: launch through onboarding or the root redirect, tap all four native tab items, reselect a tab to return to its root, and confirm the native dark background does not flash on an iPhone Simulator. Reference: [Expo Router Native Tabs](https://docs.expo.dev/router/advanced/native-tabs/).
- [x] Build only the shell compositions used now: `AppScreen`, `AppSection`, and `RecordingControl`, using the installed RNR `Text` and `Button` plus `card`/`separator` if the accepted shell needs them. Acceptance: Record, empty Modes, History, and Settings share the same page spacing and native-tab clearance; no duplicate local primitive exists; RNR doctor passes; dark screenshots are checked on the named TimberVox iPhone 17 Pro Simulator. Later slices add their own controls when they become interactive. Reference: [RNR component documentation](https://reactnativereusables.com/docs).
- [x] Consolidate the iPhone page geometry in `components/app/app-layout.ts`: 18-point page gutters/top inset, 12-point ordinary stacks, 8-point deliberately dense lists, 17-point card content, 44-point minimum controls, shared form-sheet spacing, and shell-owned safe-area/tab clearance. Remove the unused starter spacing scale so it cannot compete with the accepted shell. Acceptance: History, Modes, Personal Vocabulary, shared scrolling screens, form sheets, and bottom action bars consume the shared values; named iPhone 12 Pro Max and iPhone 17 Pro simulator screenshots remain inside safe areas.
- [ ] Audit contextual actions after the core keyboard flow is accepted. Compare the current `ActionSheetIOS` row menu with an anchored RNR Popover or Dropdown Menu across Personal Vocabulary and the rest of the app. Acceptance: the chosen pattern stays attached to its source row, respects the 44-point target and iPhone bounds, uses the root `PortalHost`, dismisses predictably, remains accessible, and replaces the action sheet only where the anchored interaction is materially clearer.
- [x] Install `jest-expo` and the Expo Router-compatible `@testing-library/react-native@13.3.3`; create `apps/ios/tests` outside `src/app`; test the root setup decision, onboarding boundary, four tab roots, tab-root reselection, current Modes empty state, and recording-control states. `renderRouter` covers route behavior; the setup redirect decision is a pure unit test. Acceptance: `pnpm test` covers only the routes and empty states that exist in this slice. Each later slice adds its own repository, component, migration, and error-state tests. Reference: [Expo Router testing](https://docs.expo.dev/router/reference/testing/).

**Playable result:** the real app launches into a native four-tab shell; each tab is independently navigable even though Modes is not functional yet.

### 2. Modes slice — create, edit, and use a mode

- [x] Replace the current module-level database setup with one `SQLiteProvider`/database module for `timbervox-mobile.db`. Add WAL mode, foreign keys, a schema-migrations table, and the `modes` plus `app_settings` repositories needed by this slice. Acceptance: a fresh database reaches the latest schema once, relaunch is idempotent, and repository tests use bound parameters. Reference: [Expo SQLite](https://docs.expo.dev/versions/v57.0.0/sdk/sqlite/).
- [x] Seed exactly one active Voice to Text mode on a fresh install. Voice to Text uses the default supported transcription route, punctuation, and zero AI post-processing. Acceptance: reinstall/fresh database creates one mode once; relaunch does not duplicate it.
- [x] Implement the Mode repository and active-mode service. Acceptance: create, read, update, delete, list, and set-active are transactional; deleting the active mode requires choosing a replacement; at most one mode is active.
- [x] Build the Modes tab as a scrollable list with a persistent Create Mode action. Build `modes/new` and `modes/[modeId]` with a centered icon/name header that enters editing only on demand, an in-card preset description, language, transcription model, speaker identification, a catalog-driven language model for processing presets, and instructions for Custom. Acceptance: ten seeded modes scroll without colliding with the bottom action; the icon sheet renders the complete dense grid; changing a preset updates the suggested icon until the user explicitly chooses an icon; every field and Delete Mode remain reachable above the native tabs and Use Mode footer; Use Mode updates the Record header immediately; reopening a stored mode starts from that row rather than a cached abandoned draft; and a stale/deleted mode route returns to the Modes list instead of hanging on `Loading mode…`. Verify the editor at its top and bottom on the named iPhone Simulator before checking this item.
- [x] Drive cloud model, language, and speaker controls from the Worker catalog and route capabilities. Show a realtime toggle only when the selected model exposes both batch and realtime routes; use the selected transport directly, and omit the control for single-route models. Keep provider-published WER presentation explicitly labeled by benchmark and separate from local-model LibriSpeech figures because those numbers are not directly comparable. Acceptance: unsupported fields are absent rather than disabled guesses; saving revalidates the selected route; the mobile app contains no independent cloud model/language matrix.
- [x] Define preset output contracts and fixtures: Voice to Text returns canonical ASR text; Message returns a concise conversational message; Mail returns an email; Note returns a structured note; Custom applies user instructions. Meeting stays unavailable until its runtime exists. Acceptance: each available preset has input, expected request, and displayed artifact fixtures.

**Playable result:** create a mode, change its icon/name/options, tap Use Mode, and immediately see that mode in the centered Record header.

### 3. Record slice — speak and receive streaming text

- [x] Extract one mode-aware DictationWorkflow used by Record, keyboard requests, and App Intent requests. Implement the explicit `idle`, `ready`, `connecting`, `listening`, `finalizing`, `result`, and `error` transitions from the architecture document. Acceptance: transition tests cover start, stop, cancel, socket failure, permission denial, provider error, final result, and retry.
- [x] Render one plain transcript surface on Record. Append real provider deltas, replace only the uncommitted portion with provider interim text, retain the last partial while finalizing, and replace it with the canonical final result. Acceptance: fixture WebSocket events prove the exact visible string after every event; no chat model or synthesized waveform participates. Reference: [Expo Audio streaming/background configuration](https://docs.expo.dev/versions/v57.0.0/sdk/audio/).
- [x] Extend the migrated SQLite database with `dictations` and `artifacts`. Assign one request ID before capture starts and one durable result ID when finalization succeeds. Persist the Dictation, Raw Artifact, active mode snapshot, model/language, duration, word count, and entry point in one transaction.
- [x] Use the same mode-aware workflow for genuine cloud batch, cloud realtime, local batch, and local realtime execution. Deliver only after persistence; app and Shortcut delivery copy the canonical text, keyboard delivery publishes the matching App Group result, and a successful delivery clears the live Record surface after brief `Copied` feedback while History retains the result. Acceptance: workflow tests cover each executor family, independent save/delivery retry, and result acknowledgement back to `ready`.
- [x] Add the source-owned `TimberVoxLocalAsr` Expo module around FluidAudio `v0.15.5`. Treat Parakeet TDT-CTC 110M batch plus Parakeet EOU 120M/320 ms realtime as one downloadable local package with one progress/ready state, then dispatch according to the mode's realtime preference. Acceptance: the generated iOS project autolinks the module, CocoaPods pins the reviewed FluidAudio tag, and the named iPhone Simulator compiles the native module. Physical-device performance and accuracy remain the separate spike below.
- [ ] Keep the completed recording under `Paths.document/recordings` through `expo-file-system`; use a request/result-derived filename and store file size/format with the Dictation. Acceptance: one in-app dictation produces one final transcript, one Raw Artifact, and one referenced WAV that survive relaunch. Reference: [Expo FileSystem](https://docs.expo.dev/versions/v57.0.0/sdk/filesystem/).
- [ ] Build Record states for microphone denial, missing mobile session, connection timeout, interrupted input, provider error, and persistence failure. Acceptance: each state preserves recoverable text and exposes one concrete action—open Settings, sign in/retry session, reconnect, or retry save.

**Playable result:** choose a mode, record in the app, watch genuine partial text only for realtime routes, stop, receive `Copied` delivery feedback, and inspect the durable result in History after the Record surface resets.

### 4. History slice — find, inspect, play, and delete that dictation

- [ ] Migrate every existing `dictation_history` row into normalized Dictation and Raw Artifact records while retaining its `audio_uri`. Acceptance: seeded pre-migration data keeps the same text, timestamp, duration, model, entry point, and playable recording after migration.
- [ ] Build History as a searchable list of real Dictation rows. Each row shows transcript excerpt plus one metadata line containing date/time, word count, and duration, and opens `history/[dictationId]`. Acceptance: short and long transcripts, empty history, search with zero results, and 100 seeded rows are verified on iPhone Simulator.
- [ ] Build the detail route with transcript text directly on the page and bottom Raw, Segmented, and Processed artifact tabs. Raw always means canonical final ASR; Segmented appears only with timed segments/words; Processed appears only when mode processing produced it. Acceptance: fixtures cover all valid artifact combinations. Reference: [RNR Tabs](https://reactnativereusables.com/docs/components/tabs).
- [ ] Implement playback with `useAudioPlayer` and `useAudioPlayerStatus`, using real elapsed time, duration, seeking, and end state. Acceptance: persisted WAV playback, pause/resume, seek, missing file, and deleted audio are exercised; a waveform is added only if real peaks are later persisted. Reference: [Expo Audio playback status](https://docs.expo.dev/versions/v57.0.0/sdk/audio/#useaudioplayerstatusplayer).
- [ ] Implement share, delete, info, and reprocess actions. Acceptance: deletion uses the native iOS destructive confirmation and removes the selected artifacts/files transactionally; reprocess creates a linked artifact/run rather than overwriting the source.
- [ ] Add a retention service used after recording, on launch, and after a retention-setting change. Acceptance: Never, 1 day, 7 days, 30 days, and 90 days policies delete expired audio while preserving transcript artifacts; Clear Audio and Clear History remain separate confirmed actions.
- [ ] Persist `entryPoint` as `app`, `keyboard`, or `shortcut` and render it only where product-relevant. Acceptance: the schema and UI make no claim about the destination host app because the public custom-keyboard API does not expose that identity.

**Playable result:** open the dictation created in Slice 3 on its own page, switch among real artifacts, play its real audio, share it, and delete it.

### 5. Keyboard slice — dictate into another app

- [x] Version the App Group bridge under `group.studio.peacockery.timbervox`. Store keyboard/access facts, active mode ID, keyboard preferences, recording/request state, partial text, and final result ID. Acceptance: old keys migrate once and Swift/TypeScript contract tests share one schema version/key list. Reference: [`@bacons/apple-targets` App Group storage](https://github.com/EvanBacon/expo-apple-targets#sharing-data-between-targets).
- [ ] Connect the Swift keyboard to the versioned bridge and active mode. Acceptance: keyboard installed/seen, Full Access, mode ID, recording state, partial text, and final result ID round-trip between Swift and the host app after relaunch.
- [ ] Establish a physical-iPhone keyboard baseline covering tap typing, visible swipe trail, decoder accuracy corpus, three predictions, correction selection, haptics, secure-field fallback, globe key, and final text insertion. Record measured failures separately from implemented behavior.
- [ ] Finish Personal Vocabulary as a searchable, virtualized full page over the shared versioned App Group document. Learned rows retain usage count and recency; dragging creates one fixed-slot anchor while automatic entries continue ranking in the gaps; the compact row menu provides Move to Top, Resume Automatic Ranking, and Forget. Acceptance: v1 manual ranks migrate once, counts remain visible and survive pin/resume/relaunch, drag autoscroll works through 500 entries, filtered search stays responsive, Swift and TypeScript contract fixtures agree, and the named iPhone Simulator visually verifies empty, populated, filtered, menu, confirmation, and reordered states. Reference: [Legend List](https://legendapp.com/open-source/list/v3/react-native/getting-started/).
- [ ] Insert only the final transcript associated with the keyboard request/result ID and mark it consumed once. Acceptance: duplicate polling cannot insert twice; a stale result from another request cannot insert; failure leaves typed text untouched.
- [ ] Add a local Keyboard Statistics page after the core keyboard acceptance passes. Present measured usage separately from counterfactual estimates. Measured counters include delivered characters and words, active keyboard duration, voice capture/finalization duration, tap keypresses, swipe commits, accepted predictions, corrections, and backspaces. Estimate keystrokes saved from delivered characters minus physical input actions; estimate time saved from an explicit configurable baseline typing WPM versus the corresponding measured active duration, with separate tap/swipe and voice breakdowns. Acceptance: the UI labels measured and estimated facts distinctly; counters survive relaunch; reset is confirmed; negative savings clamp to zero; duplicate delivery cannot double-count; and deterministic clock fixtures prove every displayed total.

**Playable result:** stay in Notes, use the TimberVox keyboard to start/stop, speak, and receive the matching final result exactly once.

### 6. Shortcut slice — dictate without foregrounding TimberVox

- [ ] Create the minimal signed `AudioRecordingIntent` and Live Activity targets with `@bacons/apple-targets`. Acceptance on a physical iPhone logs the executing bundle/process, JavaScript startup, audio-stream lifetime, lock-screen behavior, suspension, force-quit behavior, and network interruption. References: [Apple AudioRecordingIntent](https://developer.apple.com/documentation/appintents/audiorecordingintent), [Apple LiveActivityIntent](https://developer.apple.com/documentation/appintents/liveactivityintent), [`@bacons/apple-targets` target generation](https://github.com/EvanBacon/expo-apple-targets).
- [ ] Invoke the intent a second time to stop/finalize and return the canonical transcript string without presenting the app. Acceptance: the exact two-invocation contract passes from Shortcuts and the Action button with the phone locked and unlocked.
- [ ] Record the device-spike result in the architecture decision: either keep Expo Audio/Worker transport in TypeScript behind the native intent bridge, or implement one native Expo recording coordinator. Acceptance: only one production capture/transport owner remains.
- [ ] Publish a TimberVox App Shortcut and an optional wrapper that copies the returned transcript, shows a notification, and vibrates. Acceptance: onboarding opens the TimberVox shortcut surface and a fresh install can add/run it. Reference: [Apple App Shortcuts](https://developer.apple.com/documentation/appintents/app-shortcuts).

**Playable result:** invoke TimberVox from Shortcuts or the Action button once to start and again to stop, receive the final transcript, and remain in the original app.

### 7. Account and setup slice — make the working app releasable

- [ ] Add a Worker endpoint that exchanges authenticated installation/account proof for a scoped, revocable mobile session token. Acceptance: revoke, expiry, re-mint, reinstall, and rate/usage attribution integration tests pass.
- [ ] Keep the mobile session token in memory and re-mint it from installation/account proof after launch or expiry. Remove the disposable `peacockeryVoiceApiKey` path after that flow lands. Until then, only local, preview, and `testflight-dev` builds opt into the ignored `.env` key; the `production` profile omits it. Acceptance: production-config inspection contains no static service key or persisted credential and realtime/catalog/upload requests re-mint once after expiry.
- [ ] Build resumable onboarding from observed microphone, keyboard installed, Full Access, App Group, App Shortcut, and completed test-dictation state. Acceptance: every check changes only after real evidence and each incomplete row opens the correct recovery destination.
- [ ] Build Settings sections for Keyboard, Dictation, Shortcuts, Storage & Privacy, Account, Access Status, and About. Acceptance: keyboard preferences mirror to App Group; retention invokes the storage service; Account opens entitlement details; green Active renders only from verified entitlement data.

**Playable result:** install fresh, complete truthful setup, authenticate without an exposed key, configure retention/keyboard preferences, and inspect real account/access state.

### 8. TestFlight slice — install and accept the release build

- [ ] Regenerate the ignored iOS project; verify App Group, keyboard, Live Activity, and App Intent entitlements/signing; build Release; upload to App Store Connect; install from TestFlight; and run the full physical-iPhone matrix. Acceptance state is reported separately as release-built, uploaded, installed, and device-verified.

**Current release state (2026-07-19):** TimberVox 1.0.0 (88) is release-built on Team Mac, exported with explicit App Store profiles for the app, keyboard, Live Activity, and App Intent bundles, uploaded successfully to App Store Connect, and reports `VALID` plus `IN_BETA_TESTING`. The internal group has all-build access and contains both existing App Store Connect team accounts. The iPhone 17 Pro Simulator passes evidence-driven onboarding status, real Apple Shortcut import/replacement, V-O-X-T-R-A-L connect/listen/finalize with durable no-speech history across relaunch, external-app keyboard selection, tap/symbol typing, three predictions, correction selection, continuous swipe decoding, globe switching, and the combined Parakeet 110M batch plus EOU 120M/320 ms package download/ready lifecycle. TestFlight installation and the physical-iPhone microphone, exactly-once keyboard insertion, Shortcut/Action-button lock-state, swipe-trail quality, and FluidAudio performance/accuracy matrix remain separate acceptance states.

**Playable result:** download the signed build from TestFlight and repeat the Record, Modes, History, keyboard, and Shortcut paths on the physical iPhone.

## Now: close the cloud-dictation alpha

### Runtime reliability

- [ ] Run a hands-on default-microphone disconnect/reconnect acceptance when convenient. Bluetooth format changes are later device-matrix coverage and do not block the cloud-dictation alpha without the hardware.
- [ ] Exercise system-audio denial/revocation/re-grant, network interruption, and quit/relaunch recovery. Confirm the permission evidence flag changes only after non-silent system capture.
- [ ] Human-check the remaining playback paths: Mute during a real dictation and Pause with Music or Spotify, including the first Automation permission prompt. Verify the release entitlement/justification before App Store submission.

### Prompt, context, and persistence

- [x] Replace untyped/partial transcription results with one versioned `TranscriptionArtifact` across Worker batch/realtime, cloud Swift, and FluidAudio batch/realtime. Preserve provider-native JSON, timing collections and availability, scores/log probabilities, language, metrics, usage, provenance, and warnings. The OpenAPI `JobView.result` and realtime terminal `result` reference only this schema; no legacy result fallback exists.
- [x] Require every new Mac dictation and History rerun to persist the complete artifact and derive only searchable product projections from it. Timed segments are read from the artifact rather than duplicated in `segmentsJSON`; the scalar timing projection is explicitly transcription wall time rather than a provider/processing/wall substitution. Preserve any migrated pre-artifact latency/segment values under explicit legacy names for inspection/export without writing them on new rows. Historical external imports remain explicitly artifact-less because their source did not provide one.
- [x] Run dictation text processing through the versioned Worker SSE contract and persist the request, every received stream event, terminal outcome or failure, usage, warnings, provider performance, and wall time as one embedded transformation capture on the dictation record rather than introducing a second artifact identity. `POST /v1/text` remains the nonstreaming object/text operation.
- [x] Persist completed Mac attempts in the existing `transcripts` row as `succeeded`, `no_speech`, or `failed`, with structured error code/message, preserved recording metadata, the complete transcription artifact whenever the runtime produced one (including no-speech and failed realtime terminal artifacts), and the failed transformation capture when processing began. History is the product surface for these outcomes; Home does not render a separate error notice.
- [x] Run one real transformed dictation through the deployed `/v1/text/stream` route and inspect the saved success/failure event capture. Worker version `dc2135d5-a9ca-4762-a982-bcd281342f5e` was deployed and passed pre- and post-deploy integration checks on 2026-07-14. The first launched-app run preserved a real HTTP 400 failure capture and exposed that the Swift client was snake-casing the Worker's camel-case text request. After the connector gained explicit camel-case encoding for `/v1/text` and `/v1/text/stream`, the second launched-app run saved the raw transcript, complete stream events/outcome, Google/Gemini provider data, and processed text.
- [ ] Run controlled macOS context acceptance and save inspectable artifacts: application/window/focused text, selected text, clipboard inside and outside the three-second pre-recording boundary, clipboard changes during recording, and copied file/image metadata.
- [x] Retire active ScreenCaptureKit screenshot plus Vision OCR capture while retaining historical `screenText` and screen-image attachment decoding for old records and research evidence.
- [ ] Verify persisted context capture in a launched dictation. A launched Custom-mode run captured one synthetic clipboard item, used it to add the synthetic `Redwood Lantern` project name to the processed result, persisted the snapshot on the existing History row, and projected `Clipboard, 1 item` into Recording information. A text-processing failure snapshot was also preserved. Runtime acceptance for application/selection/file context plus no-speech and transcription-failure outcomes remains.
- [ ] Run one complete transformed dictation from recording through real provider, paste delivery, saved raw/final text, saved context, and History inspection. Recording, cloud batch transcription, Gemini SSE processing, raw/final/context persistence, History artifact tabs, and Recording information were verified together in the launched app on 2026-07-14; external-app paste delivery remains to be accepted.

### macOS behavior acceptance

- [ ] Cold-launch and verify Microphone, Accessibility, and System Audio Recording independently, including denial, Settings recovery, revocation, and re-grant.
- [ ] Verify global toggle, stop, and cancel while the main TimberVox window is closed.
- [ ] Verify paste and clipboard restoration in TextEdit plus a browser/editor while TimberVox remains inactive. With `Keep transcript on clipboard` disabled, three launched cloud dictations restored the exact pre-recording TextEdit clipboard sentinel after delivery on 2026-07-14. External auto-paste remains unaccepted because those automation runs started and stopped from TimberVox rather than through the real global shortcut/menu-bar path with the target app still frontmost; browser/editor coverage also remains.
- [ ] Verify the passive recording indicator never activates TimberVox or steals focus.
- [ ] Verify History survives quit/relaunch, search finds a saved run, playback works, and rerun retains lineage instead of appearing as an unrelated dictation.

### Production UI integration

- [x] Remove the separate sample UI and Xcode canvas paths. UI iteration happens in the production app with real runtime state.
- [x] Establish the copy-owned SwiftCN lane under `Features/Components/UI` with the initial Theme, Button, Card, Item, and Separator source units. TimberVox does not depend on the SwiftCN package.
- [ ] Replace one-off production styling with reviewed UI primitives and named reusable composites as each screen is reconciled. Keep one primary component per Swift file and validate interactions in the built app.
- [ ] Reconcile the accepted external UI mock-up with the production Home, History, Settings, onboarding, and navigation one surface at a time. Record interaction decisions here or in `UI-DESIGN.md`; do not restore sample-backed app surfaces.
- [x] Reconcile production Settings with the ui-prototype card layout on shared `AppSettingsCard`/`AppSettingsRow` composites: Appearance, Recording indicator (live style previews), Keyboard shortcuts, Clipboard, Local models, History retention, Application, and the permissions Access grid. The unreleased Plans surface stays absent until the separate billing lane passes rather than rendering RevenueCat configuration failures in Settings. 2026-07-20.
- [x] Audio-recording retention shipped in Settings → History: "Keep audio recordings" window (default Forever — deleting audio is opt-in), storage-used measurement across Recordings and ContextAttachments, and a confirmed Clear-recordings action disabled while dictating. Launch sweep covers both directories. 2026-07-15.
- [ ] Decide whether a "don't save audio at all" workflow toggle is wanted (the ui-prototype's "Keep audio recordings" switch). That changes the dictation workflow to discard the WAV after transcription, which also removes re-transcribe — needs a product call before building.
- [x] Fold Modes' card and row anatomy into the shared `AppSettingsCard`/`AppSettingsRow` composites while preserving the accepted regular row sizing and inline help behavior. Remove the duplicate Mode-only wrappers. 2026-07-15.
- [x] Reconcile production Home around the shared page inset and spacing scale, including the compact Today card header-to-content gap. Verified in the built production app with populated history on 2026-07-14.
- [ ] Finish production Modes interaction acceptance. The reference-aligned collection, full-page detail, always-visible audio controls, and trailing app-activation sheet use the real `ModeStore`, Worker/local model catalog, provider marks, persisted app activation, and source-app mode resolution. Verify arrowless anchored Combobox/HoverCard presentation above cards and scroll views, opaque popup chrome, click-away/Escape dismissal, keyboard search/selection, the shared 200-point control width, create/use/duplicate/delete, custom prompt/context, app selection, and supported window sizes. The reference-aligned inline Custom instructions, four copied SwiftCN context checkboxes, Context information popover and Escape dismissal, Use Mode/Active state, app-activation sheet, delete confirmation, and 1000-by-620 layout were verified in the launched app on 2026-07-15. Store tests cover active-mode persistence plus duplicate/delete state transitions; live create/duplicate/final-delete acceptance remains.
- [x] Validate model-picker actions end to end. Favorites persist and regroup the list; local-model download/prepare and reference-aware delete call `FluidAudioModelPackageStore`; busy rows are disabled; and the app-level footer reports byte-weighted progress for one or several downloads. A real Songbird install ran from its circular download action through the three-asset package, showed approximately `483.1 MB of 1.76 GB` at 27%, reached `1.76 GB of 1.76 GB` at 100% while Core ML preparation finished, dismissed the global footer only after verification, and ended as `Ready · 1.79 GB` on 2026-07-15. Live inspection also verified favorite add/remove regrouping and restored the original state. Deterministic store tests cover concurrent shared-asset preparation, byte weighting, downloaded-but-unverified preparation, failure, and reference-aware shared-asset retention.
- [x] Define and implement a versioned `.timbervoxmode` sharing contract. Schema version 1 exports through Share, imports through the Modes collection, rejects oversized or unsupported files, normalizes imported catalog selections, assigns a fresh ID, resolves name conflicts, and round-trips in tests. 2026-07-15.
- [ ] Remove sample-only behavior from every promoted surface. Transcriptions, Meetings, and Commands must remain absent from production navigation until their runtime paths exist.
- [ ] Finish production History as one accepted surface. The source now separates collapsed disclosure rows from selectable expanded transcripts, exposes Raw/Segmented/Processed views only when their artifacts exist, and uses the reviewed SwiftCN Transcript Viewer and Scrub Bar ports for one shared real-audio clock across inline and detail playback. Word timing highlights individual clickable words and seeking starts at the selected word; segment-only timing highlights and seeks the active segment. Recording information includes summarized captured context, actual ASR RTFx, and effective end-to-end AI text speed. Failed, no-speech, missing-recording, playback, detail, and rerun states remain inside History. The existing `AppSearchHeader` remains the intentional shad-style composite built from copied SwiftCN primitives; it is not being replaced with `.searchable`. Click-to-seek, playback highlighting, processed/raw/segmented tabs, the failure row, a successful processed row, and Recording information were verified in the launched app on 2026-07-14. A real missing-recording row, disabled rerun help, footer layout, transcript search/highlighting, Recording information, diagnostic-export placement, and the 1000-by-620 layout were verified in the launched app on 2026-07-15. No-speech remains unaccepted because the current database has no no-speech record.
- [x] Add a versioned diagnostic JSON export action for the stored row, canonical transcription artifact, context snapshot, and embedded transformation capture. The action lives at the bottom of Recording information, warns what it contains, and round-trips exact acronym-bearing record keys in tests. The complete data remains out of the normal Recording information presentation. 2026-07-15.
- [ ] Visually verify the connected production window at supported sizes, light/dark appearance, empty/loading/error/populated states, keyboard navigation, and VoiceOver labels.

## Next: local models

- [x] Keep FluidAudio download, verification, shared-asset deletion, progress, and retention state together under `Core/Transcription/FluidAudio/ModelManagement`; the UI consumes the same `FluidAudioModelPackageStore` rather than a second download-state store.
- [x] Connect the local package lifecycle to production UI: show missing, partial, downloaded-but-unverified, downloading, ready, and failed states; show combined package size/progress; and offer prepare/verify and reference-aware deletion. Launched-app acceptance verified ready, partial, not-downloaded, and real active-download rows; measured package bytes; byte-weighted combined progress; Download/Finish downloading/Delete actions; global progress while navigating; `View in Modes`; and terminal ready completion for Hummingbird and Songbird. Deterministic store tests cover the unverified, concurrent, failure, and shared-asset deletion boundaries that should not require corrupting or deleting a user's real installation. 2026-07-15.
- [ ] Run a physical-iPhone FluidAudio spike for Parakeet EOU 120M at 320 ms and Parakeet TDT-CTC 110M. Measure cold/warm load, peak resident memory, partial/final latency, sustained thermals, battery use, and WER on the same dictation corpus. Decide whether one or both managers may remain loaded only from those measurements; the model-picker entries are evaluation UI until this passes.

## Next: language-model selection

- [ ] Define and enforce a low-latency reasoning profile for every exposed language-model route. Record the exact provider option and supported values per route, use the provider's true no-reasoning mode when available, use the minimum supported effort for reasoning-only models, and cover each mapping with a deployed integration check that produces final visible text inside the interactive timeout.
  - Deployment status (2026-07-14): Worker version `dc2135d5-a9ca-4762-a982-bcd281342f5e` contains the catalog-owned profiles and caller-override enforcement and passed the deployed integration suite. This remains unchecked until every exposed route returns visible text in the live route matrix.
- [ ] Build a TimberVox transform benchmark before showing a language-model quality score. Use the real Message, Mail, Note, and Custom fixtures; score preset/format compliance, preservation of names/numbers/intent, unsupported additions, and human-rated usefulness. Record time to first token and output throughput from the TimberVox API path separately. [IFEval](https://arxiv.org/abs/2311.07911) is the reproducible instruction-compliance reference and [WritingBench](https://arxiv.org/abs/2503.05244) is the writing-quality reference, but neither replaces transcript-to-artifact faithfulness testing.
- [ ] Deploy and visually verify the versioned model-presentation contract. Source schema version 1 now carries voice-model WER/realtime speed and language-model effective end-to-end TPS plus the matching Artificial Analysis index/profile/version/date. Mac and iPhone consume the same fields; local model combinations use their final batch model's FluidAudio LibriSpeech WER and say `Realtime` when the package includes a realtime route. Mac source attribution and customer-facing data-use-term confirmation remain.
  - Deployment status (2026-07-14): schema version 1 is live in Worker version `01a827a7-1f76-41c7-94b0-ee5b88ad555d`, and the deployed integration suite passed after release. Both clients require literal schema version 1 and reject missing or unsupported presentation contracts. The Intelligence Index conversion is fixed rather than catalog-relative: source index divided by ten, rounded to one display decimal. The live catalog exposes intelligence metadata for all ten configured language models. The launched iPhone picker displayed all ten ratings, including Mistral Large at `1.6/10`, Medium at `3.0/10`, and Small at `1.1/10`, plus provider icons, effective TPS, and the source/method explanation. Artificial Analysis v4.1 snapshots retain their source profiles; by explicit product decision, Mistral Medium 3.5 uses its published reasoning-profile score while the TimberVox route remains configured with reasoning disabled. The launched Mac picker previously verified the shared metric presentation; a visible Artificial Analysis source explanation remains to be added there.
- [ ] Add an optional personal route benchmark that runs a bounded, versioned transform fixture through the currently available TimberVox language-model routes. Report successful completion rate, effective visible-text tokens per second, time to first visible text, and failure class; retain route, model, reasoning profile, date, and connection context. Acceptance: published and personal measurements remain separately labeled, only completed visible-text samples are ranked, and the result recommends the fastest successful route for the current connection.
- [ ] Normalize language-model stream failures into typed terminal metadata with upstream status, provider code, failure class, and retry-after duration when available. Acceptance: clients can distinguish rate limits, provider timeouts, authentication failures, invalid requests, and transient upstream failures without parsing human-readable messages, and retry scheduling honors the provider's supplied delay.
  - Local status (2026-07-14): `/v1/text/stream` and the benchmark now preserve normalized categories, status/provider codes, retryability, and standard retry-after headers. A reasoning-only completion is a distinct non-retryable `empty_output` failure rather than a false success. This remains unchecked until deployed error-path integration verifies representative provider failures and the consuming retry workflow honors the delay.

## Then: durable transcription and meetings

- [ ] Before implementing File or Meeting persistence, define its concrete workflow and inspection/export requirements, then approve the smallest storage shape that satisfies them. Do not infer a second run object, one column per metric, or table-per-collection storage from the dictation schema.
- [ ] Port TXT, Markdown, JSON, SRT, and WebVTT export with generated-artifact verification. Add HTML/PDF/DOCX only when implemented and visually checked.
- [ ] Build File Transcription before Meetings: import, progress, cancellation, editing, speaker renaming, playback seeking, rerun, persistence, and export using the existing direct-R2 path.
- [ ] Build Meetings on the accepted microphone-plus-system capture: retained stems/master, checkpoints, provisional realtime text, final batch reconciliation, and summaries generated only from the final transcript.
- [ ] Run the existing AMI two-speaker fixture, a multipart-sized file, and a 10–15 minute live meeting through interruption, quit/relaunch, reconciliation, editing, and export.

## Cloud and billing before release

- [x] Publish and consume the canonical Peacockery Voice Swift device-bound credential middleware. The macOS release creates one per-install Secure Enclave P-256 DPoP key, registers a TestFlight or App Store receipt, stores only the short-lived JKT-bound `pvc_` credential and installation identifier, and sends fresh proofs across catalog, text, upload/batch, and realtime authorization with nonce retry. Build 89 was installed from TestFlight on 2026-07-20; the production Worker recorded its valid sandbox receipt enrollment and an authenticated DPoP request, while fake-receipt and replay probes were rejected. The release no longer embeds or reads the parent credential. App Attest remains a separate supported-platform registration route rather than the macOS 26 bootstrap.
- [ ] Finish destructive and time-dependent device-credential acceptance: revoke an enrolled installation, confirm the next request is rejected, confirm bounded re-enrollment behavior, and exercise natural token expiry/refresh. Keep those states distinct from the completed TestFlight receipt, hardware-key, nonce, and replay evidence.
- [ ] Add static-API-key-scoped upload/transcription rate limits and usage quotas grounded in actual provider and R2 constraints.
- [ ] Repair RevenueCat Test Store products/packages in the app, verify $7.99 monthly and $19.99 lifetime, then accept purchase success/cancel/failure, restore, and universal purchase after the iOS app exists. RevenueCat does not provision or authorize Worker access.
- [ ] Revalidate the App Store Connect Team API key after Apple's upstream `401 NOT_AUTHORIZED` clears; do not generate more keys merely to chase propagation.
- [ ] Add App Review metadata/screenshots and verify the release sandbox, signing, Apple-events behavior, and RevenueCat production key.
- [ ] Rotate the exposed Cloudflare API token and R2 S3 credentials, replace Worker secrets with bucket-scoped credentials, rotate the static TimberVox API key set, and rerun direct-upload smoke.

## Later product slices

- [ ] Sound feedback as a complete settings-and-runtime slice.
- [ ] Hot mic and push-to-talk with explicit buffer, permission, and cancellation semantics.
- [ ] Vocabulary/dictionary integrated into actual context capture and transform acceptance.
- [ ] Voice Commands only after command recognition, confirmation, execution, persistence, and recovery exist as a real runtime.
