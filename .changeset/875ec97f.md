---
"toy-local-app": patch
---

Allow recording/transcription to start without paste permission preflight, keep hotkey capture active when Accessibility is denied by requiring only Input Monitoring for event taps, improve always-on paste reliability by removing System Events AppleScript fallback and using Cmd+V plus AX insertion only, smooth the always-on indicator meter animation, and avoid opening Accessibility settings during automatic paste attempts when post-event access is unavailable
